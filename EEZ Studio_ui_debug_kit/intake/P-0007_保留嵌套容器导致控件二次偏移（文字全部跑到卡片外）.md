# P-0007 · 保留嵌套容器导致控件二次偏移（文字全部跑到卡片外）

- **工程**：（待补）
- **日期**：2026-09-23
- **工具**：WorkBuddy
- **状态**：fixed
- **标签**：lvgl,d2
- **关联提示词**：（无）

## 现象（看到什么）

仿真图上卡片里只剩一条滚动条，文字/图标全部不在原位

## 复现（怎么稳定重现）

（待补）

## 根因（真正的原因）

LVGL 子控件坐标相对父对象内容区；DSL 写的是 800×480 绝对坐标，嵌套后又被父容器位置叠加一次，还要再减父容器 padding/border

## 修复（做了什么）

json2eez 里 flatten_screen()：所有后代提升为 screen 直接子节点（先序=原绘制顺序），并给所有容器显式 pad_*=0

## 证据（数字 / 命令输出）

同一份 DSL：平铺前 01 屏文字不可见，平铺后与设计稿结构一致

## 沉淀（新增断言 / 案例 / 文档）

（待补）
