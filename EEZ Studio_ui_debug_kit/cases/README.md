# cases/ —— 案例库

**这里是这个工具箱最值钱的部分。** 每解决一个非平凡问题，就追加一个案例文件。

## 为什么要写案例

同一个坑会在不同工程里反复出现（只是表现略有不同）。案例库让"下一次"变成**对号入座**，
而不是重新推理一遍。特别是**记录曾经的误判**比只留正确答案更有价值 ——
它能让后来者避开同一条弯路。

## 文件命名

```
B<序号>_<一句话现象>.md          例：B3_默认主题padding偏移.md
```

按发现顺序编号，不要重排（编号是引用锚点）。

## 必须包含的 7 段（模板）

```markdown
# B<n> <现象一句话>

## 现象
（客观描述：哪一类界面、哪个元素、什么表现。带上可复现的位置/尺寸）

## 证据
（数值！像素计数 / 墨迹包围盒 / 坐标比对 / 报错原文。没有数值证据的判断不算证据）

## 曾经的误判
（如果走过弯路，写清楚：误判成什么、为什么那样想、什么证据推翻了它。没有就写"无"）

## 真因
（一句话说清机制。要能解释"为什么表现是这样的"，而不只是"改了什么就好了"）

## 修法
（改哪个环节的哪一处；给出关键代码片段；说明为什么这样改是根治而不是打补丁）

## 验证
（用什么命令、什么数值，证明修好了。要能被别人复现）

## 通用化
（这个坑在别的框架/栈里对应什么？给出跨栈对照，方便迁移）
```

## 判据：什么值得写成一个案例

- 花了 **>30 分钟** 才定位的问题
- 有过 **≥1 次误判**的问题
- 根因属于某类**通用机制**（默认样式、异步、坐标系、字体资源…）
- 修复过程可以**脚本化/自动化**

## 索引

| 编号 | 现象 | 根因类别 | 对应 PLAYBOOK |
|---|---|---|---|
| [B1](B1_字体源分配错误.md) | 图标全空白 / 正文变方框 | 资源分配（字体源） | §4.4 |
| [B2](B2_保存时重算坐标.md) | 元素跑出画布边缘 | 工具保存时重算布局 | §4.5 |
| [B3](B3_默认主题padding偏移.md) | 某类控件内子元素整体偏移 | 默认主题 padding | §4.2 |
| [B4](B4_异步渲染拿到旧帧.md) | 批量抓图内容重复 | 异步渲染 | §4.6 |
| [B5](B5_子控件二次偏移（嵌套容器）.md) | 卡片里只剩滚动条，文字跑到卡片外 | 坐标系（子坐标相对父内容区） | §4.10 || [B6](B6_样式值写错语法被静默吞掉.md) | GUI 报 invalid color，CLI 构建说零错误 | 静默失效（取值语法） | §4.9 |
| [B7](B7_构建成功却零产出.md) | 构建"成功"但一个产物都没有 | 配置驱动的产出清单 | §4.7 |
| [B8](B8_仿真器出图的四个坑.md) | 导出图颜色错 / 快照内存不足 / 进程不退 / 改配置不生效 | 像素字节序·内存·进程·配置路径 | §3.1 / §5.6 |
| [B9](B9_图标在父对象里没居中（手摆像素vs声明式居中）.md) | 圆形按钮里图标偏左；与编辑器「居中」结果差 1px | 居中未被声明（手摆像素）+ 舍入模式 half-even vs half-up | §4.2 / §5.7 |
| [B10](B10_布局助手给最后一项多加了一个间隔（纯图标偏中心）.md) | 只放图标的胶囊按钮，图标左边窄右边宽（恒定差 6px） | 分隔符语义：尾随间隔 vs 项间间隔 | §4.2 |

> B5 与 B3 同族：都是「父容器的样式悄悄改了子控件位置」。
> B3 是**知道偏移量然后补 0**，B5 是**坐标系基准**问题 ——
> 它的修法有两个方向（**平铺** / **分层+相对换算**），
> 最终结论与精确换算式子见 B5 文末的「补充」。（默认选分层+换算，平铺留作排障。）

> **B9 与 B10 表现极像（都叫"图标没居中"）却机制不同**，排查时先用「差多少」区分：
> 偏移量**恰好等于某个间隔常量的一半**、且只偏一个方向 → 看 B10（尾随分隔符）；
> 偏移量随父尺寸/字体变化，或稳定差 1px → 看 B9（居中未声明 / 舍入模式不一致）。

> 案例里出现的具体数值与框架名称只作为**例证**与**跨栈对照**，不代表任何特定工程。
